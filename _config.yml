text_skin: default # "default" (default), "dark", "forest", "ocean", "chocolate", "orange"
highlight_theme: tomorrow-night # "default" (default), "tomorrow", "tomorrow-night", "tomorrow-night-eighties", "tomorrow-night-blue", "tomorrow-night-bright"
url     : # https://ctensz65.github.io
baseurl : # ctensz65.github.io
title   : Cartensz1965
description: > # this means to ignore newlines until "Language & timezone"
  Blog for cybersecurity articles, mainly focus on offensive security.

lang: en
timezone: Asia/Bangkok

author:
  type      : # "person" (default), "organization"
  name      : Cartensz
  url       : https://ctensz65.github.io
  avatar    : # path or url of avatar image (square)
  bio       : Security Consultant - Red Team
  email     : 
  facebook  : 
  twitter   : ctensz65 
  telegram  : 
  medium    : '@ctensz65' 
  linkedin  : neil-a-21aa5b12b
  github    : ctensz65 
  npm       : 

repository: ctensz65/ctensz65.github.io
repository_tree: master

paths:
  root    : # title link url, "/" (default)
  home    : # home layout url, "/" (default)
  archive : # "/archive.html" (default)
  rss     : # "/feed.xml" (default)

excerpt_separator: <!--more-->

license: CC-BY-NC-4.0 # "CC-BY-4.0", "CC-BY-SA-4.0", "CC-BY-NC-4.0", "CC-BY-ND-4.0"

toc:
  selectors: # "h1,h2,h3" (default)

mathjax: # false (default), true
mathjax_autoNumber: # false (default), true

mermaid: # false (default), true

chart: # false (default), true

paginate: 8
paginate_path: /page:num # don't change this unless for special need

sources: # bootcdn (default), unpkg

sharing:
  provider: false # false (default), "addtoany", "addthis", "custom"

  ## AddThis
  addthis:
    id: # AddThis pubid, e.g. ra-5xxxxxxxxxxx

comments:
  provider: false # false (default), "disqus", "gitalk", "valine", "custom"

  disqus:
    shortname: # the Disqus shortname for the site

  gitalk:
    clientID    : # GitHub Application Client ID
    clientSecret: # GitHub Application Client Secret
    repository  : # GitHub repo
    owner       : # GitHub repo owner
    admin: # GitHub repo owner and collaborators, only these guys can initialize GitHub issues, IT IS A LIST.
      # - your GitHub Id

  valine:
    app_id      : # LeanCloud App id
    app_key     : # LeanCloud App key
    placeholder : # Prompt information
    visitor     : # false (default)
    meta        : # "[nick, mail, link]" (default) nickname, E-mail, Personal-site

pageview:
  provider: false # false (default), "leancloud", "custom"

  leancloud:
    app_id    : # LeanCloud App id
    app_key   : # LeanCloud App key
    app_class : # LeanCloud App class

search:
  provider: default # "default" (default), false, "google", "custom"

  google:
    custom_search_engine_id: # Google Custom Search Engine ID

analytics:
  provider: false # false (default), "google", "custom"

  google:
    tracking_id : # Google Analytics id for the site
    anonymize_ip: false # Anonymize IP tracking for Analytics

markdown    : kramdown
highlighter : rouge
permalink   : date

exclude:
  - CHANGELOG.md
  - HOW_TO_RELEASE.md
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README-*.md
  - README.md
  - gulpfile.js
  - jekyll-text-theme.gemspec
  - package-lock.json
  - package.json
  - /docs
  - /node_modules
  - /screenshots
  - /test
  - /vendor

defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: article
      sharing: true
      license: true
      aside:
        toc: true
      show_edit_on_github: true
      show_subscribe: true
      pageview: true

plugins:
  - jekyll-feed
  - jekyll-paginate
  - jekyll-sitemap
  - jemoji
